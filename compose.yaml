services:
  postgres:
    image: postgres:latest
    container_name: finance-postgres
    environment:
      POSTGRES_DB: finance_manager
      POSTGRES_USER: finance_db
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"